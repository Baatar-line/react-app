services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: bigbang
      POSTGRES_PASSWORD: bigbang
      POSTGRES_DB: bigbang
    ports:
      - '5432:5432'
    volumes:
      - bigbang_pg_data:/var/lib/postgresql/data

volumes:
  bigbang_pg_data:
